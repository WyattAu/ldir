# Performance Baseline v0.1.0
# Generated: 2026-05-31
# Platform: ubuntu-latest (GitHub Actions)

[metadata]
version = "0.1.0"
date = "2026-05-31T00:00:00Z"
rust_version = "1.88.0"
platform = "x86_64-unknown-linux-gnu"
profile = "release"

[benchmarks]
# Markdown parsing (ns per document)
[benchmarks.markdown_parsing]
small = { input_size = 10, mean_ns = 0 }     # Updated by actual bench
medium = { input_size = 50, mean_ns = 0 }
large = { input_size = 100, mean_ns = 0 }
xlarge = { input_size = 500, mean_ns = 0 }

# LaTeX parsing
[benchmarks.latex_parsing]
simple = { mean_ns = 0 }

# SIR compilation
[benchmarks.sir_compilation]
small = { input_size = 10, mean_ns = 0 }
medium = { input_size = 50, mean_ns = 0 }
large = { input_size = 100, mean_ns = 0 }

# Hyphenation (ns per word)
[benchmarks.hyphenation]
long_word = { mean_ns = 0 }

# Font subsetting (microseconds)
[benchmarks.font_subset]
10_glyphs = { mean_us = 0 }
50_glyphs = { mean_us = 0 }
100_glyphs = { mean_us = 0 }
500_glyphs = { mean_us = 0 }

# PDF write (microseconds)
[benchmarks.pdf_write]
small_doc = { mean_us = 0 }

[thresholds]
# Fail if benchmark exceeds baseline by this percentage
warn_percent = 10
fail_percent = 50
