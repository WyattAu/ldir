[workspace]
resolver = "3"
members = [
    "ldir-core",
    "ldir-ir",
    "ldir-tex",
    "ldir-md",
    "ldir-pdf",
    "ldir-html",
    "ldir-vello",
    "ldir-wasm",
    "ldc",
    "ldir-dis",
    "ldir-as",
    "ldir-diff",
    "ldir-validate",
    "ldir-epub",
    "ldir-txt",
    "ldir-docx",
    "ldir-typst",
    "ldir-html-reader",
    "ldir-adoc",
    "ldir-org",
    "ldir-docx-reader",
    "ldir-opt",
    "ldir-link",
    "ldir-lsp",
    "tests",
]
exclude = ["ldir-core/fuzz"]

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
rust-version = "1.88"
repository = "https://github.com/WyattAu/ldir"
description = "A low-level document intermediate representation language for deterministic typesetting"

[workspace.dependencies]
# Internal
ldir-core = { path = "ldir-core", version = "0.1.0" }
ldir-ir = { path = "ldir-ir", version = "0.1.0" }
ldir-tex = { path = "ldir-tex", version = "0.1.0" }
ldir-md = { path = "ldir-md", version = "0.1.0" }
ldir-pdf = { path = "ldir-pdf", version = "0.1.0" }
ldir-vello = { path = "ldir-vello", version = "0.1.0" }
ldir-wasm = { path = "ldir-wasm", version = "0.1.0" }
ldir-html = { path = "ldir-html", version = "0.1.0" }
ldir-txt = { path = "ldir-txt", version = "0.1.0" }
ldir-epub = { path = "ldir-epub", version = "0.1.0" }
ldir-docx = { path = "ldir-docx", version = "0.1.0" }
ldir-typst = { path = "ldir-typst", version = "0.1.0" }
ldir-adoc = { path = "ldir-adoc", version = "0.1.0" }
ldir-org = { path = "ldir-org", version = "0.1.0" }
ldir-html-reader = { path = "ldir-html-reader", version = "0.1.0" }
ldir-docx-reader = { path = "ldir-docx-reader", version = "0.1.0" }

# Serialization (REQ-2.3)
rkyv = { version = "0.8", features = ["bytecheck"] }

# Fixed-point & math
fixed = "1.28"

# Text shaping (REQ-4.3.1)
harfbuzz-sys = "0.6"

# Font technology (REQ-6.2.4)
ttf-parser = "0.25"
fontdb = "0.21"

# Error handling
thiserror = "2.0"
anyhow = "1.0"
bitflags = "2.0"

# Tracing (REQ-8.1)
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["json", "env-filter"] }
tracing-chrome = "0.7"

# Testing & fuzzing
proptest = "1.6"
quickcheck = "1.0"
quickcheck_macros = "1.0"

# Benchmarking
criterion = { version = "0.5", features = ["html_reports"] }

# PDF
pdf-writer = "0.11"

# GPU rendering
vello = "0.3"
parley = "0.3"
wgpu = "22.1"

# WASM
wasmtime = "38"

# ECS reference
shipyard = "0.11"

# Concurrency
crossbeam = "0.8"
dashmap = "5"
rayon = "1"

# Deterministic ordered collections
indexmap = "2"

# Temp files (integration tests)
tempfile = "3"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
strip = true
panic = "abort"

[profile.bench]
opt-level = 3
lto = "thin"
codegen-units = 1
