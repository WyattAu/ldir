[package]
name = "ldir-txt"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "LDIR plain text backend — converts S-IR v2 to plain text"

[dependencies]
ldir-ir = { path = "../ldir-ir" }
