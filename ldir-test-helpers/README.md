# ldir-test-helpers

Shared test utilities for LDIR crates.

## Usage

Add to `[dev-dependencies]` in your `Cargo.toml`:

```toml
[dev-dependencies]
ldir-test-helpers = { workspace = true }
```

## Contents

- Common test fixtures and sample documents
- Assertion helpers for PDF validation
- Shared setup/teardown utilities
