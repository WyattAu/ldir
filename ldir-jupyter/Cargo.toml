[package]
name = "ldir-jupyter"
version = "0.1.0"
edition = "2024"
description = "Jupyter notebook export for ldir documents"

[dependencies]
ldir-ir = { path = "../ldir-ir" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
