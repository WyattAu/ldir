[package]
name = "ldir-html-reader"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "LDIR HTML reader frontend — converts HTML to S-IR v2"
repository.workspace = true
readme = "README.md"
keywords = ["html", "parser", "reader", "document", "input"]
categories = ["text-processing"]

[dependencies]
ldir-ir = { workspace = true }
