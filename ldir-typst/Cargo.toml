[package]
name = "ldir-typst"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "LDIR Typst frontend — converts Typst (.typ) to S-IR v2"
repository.workspace = true
readme = "README.md"
keywords = ["typst", "parser", "document", "input"]
categories = ["text-processing"]

[dependencies]
ldir-ir = { workspace = true }
